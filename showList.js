export default function showList(list) {
    console.log('\n======================');
    for (let i = 0; i < list.length; i++) {
        console.log(list[i])
    }
    console.log('======================');
}